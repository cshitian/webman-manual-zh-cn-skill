# Webman Manual Index

This skill mirrors `resource/doc/zh-cn` from `webman-manual`.
Use `SUMMARY.md` as the canonical map and read the smallest relevant page first.

Common paths:
- Setup: `README.md`, `attention.md`, `install.md`, `directory.md`, `tutorial.md`
- HTTP: `request.md`, `response.md`, `controller.md`, `route.md`, `middleware.md`, `view.md`, `static.md`, `session.md`
- Runtime: `exception.md`, `log.md`, `config.md`, `multiapp.md`, `coroutine/coroutine.md`
- Database: `db/*.md`
- Components: `components/*.md`
- Plugins: `plugin/*.md`, `app/*.md`
- Advanced: `process.md`, `di.md`, `aop.md`, `others/*.md`, `upgrade/*.md`

When a question spans several chapters, read the overview page first, then the linked detail pages.
