interface:
  display_name: "webman manual zh-cn"
  short_description: "webman v2 Chinese manual, CLI command reference, and code templates."
  default_prompt: "Use the webman skill for framework documentation, CLI operations (start/reload/restart), troubleshooting (class not found/port conflict), and code templates (controller/model/middleware/WebSocket/process)."
